<body>
<h1>Share a story:</h1>
<form action="/person/<%= @person.id %>/story/update" method="post">
<p>Choose the park this story is about:</p>
<select name="park_id">
<%Park.all.each do |park|  %>
<option value="<%= park.id %>"><%= park.name %></option>
<% end  %>
<textarea name="story" cols="60" rows="20"></textarea>
<input type="submit" value="That one time...">	
</form>
<a href="/person/<%= @person.id %>/story/<%= @story.id %>/delete"> Delete Story</a>
<a href="/person/<%= @person.id %>/stories">Return to main list.</a>
</body>
